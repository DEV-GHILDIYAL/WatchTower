# WatchTower Environment Configuration
# Copy this file to .env and configure as needed.

# Telegram Configuration
TELEGRAM_BOT_TOKEN=8841009072:AAEhbgwjGdPOHRspFAh89vCcMEvESI78byc
TELEGRAM_CHAT_ID=5777540786

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=watchtower

# Proxy Configuration (if behind a proxy, e.g. Docker Desktop)
HTTP_PROXY=http://http.docker.internal:3128
HTTPS_PROXY=http://http.docker.internal:3128
NO_PROXY=localhost,127.0.0.1,prometheus,exporter,grafana
http_proxy=http://http.docker.internal:3128
https_proxy=http://http.docker.internal:3128
no_proxy=localhost,127.0.0.1,prometheus,exporter,grafana

